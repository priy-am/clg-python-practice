# 2. Implement a circular queue using a list.
# ( Use a fixed-size list and track front and rear pointers. 
# Increment pointers using modulo operation (index + 1) % size. Detect a full queue by checking if (rear + 1) % size == front) 

